---
title: "Introspect a token"
description: "Verify the state, scopes, and claims of a Mysoleas JWT."
openapi: "/en/api-reference/openapi.json POST /oauth/v2/introspect"
---

Use introspection when a backend must explicitly verify that a token is active, know its scopes, or control its context before a sensitive operation.
